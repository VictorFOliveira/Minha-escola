# Final production release validation

Validates the final main state: migrations, tests, performance, TypeScript, production build, HTTP regression, dependency audit, CodeQL and the 600-request mixed-role load test.
